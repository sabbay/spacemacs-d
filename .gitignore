# Byte-compiled emacs lisp
*.elc

# Backup / autosave / lock files
*~
\#*\#
.\#*

# Spacemacs machine-specific environment cache
.spacemacs.env

# Eldev workspace (downloaded deps, byte-compile cache)
.eldev/

# Runtime telemetry log (claude-collab MCP calls — see Eldev for cap rules)
.claude-collab.log.jsonl
.claude-collab.log.jsonl.1

# Auto-generated by `update-directory-autoloads' / package.el — never commit
*-autoloads.el

# Per-machine tooling caches (Claude Code launch config, Playwright MCP logs)
.claude/
.playwright-mcp/

# Loose project-root scratch (screenshots, link dumps, ad-hoc notes)
steel_site_full.png
useful-link.md
